import React from "react";
import { Filterbar } from "../components";

const Header = () => {
  return (
    <header className="header">
      <h1>simple-notes-with-react-readux</h1>
      <Filterbar />
    </header>
  );
};

export default Header;
